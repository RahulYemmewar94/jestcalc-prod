
import { render, screen, fireEvent } from "react-test-renderer";
import CalculatorWithJestTestCases from "./calculator";

describe("Calculator Test Cases", () => {
  test("Addition", async () => {
    render(<CalculatorWithJestTestCases />);
    
 
  });
});
